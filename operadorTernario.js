let usuario = 'Emilia';

// En el caso que sea igual "?" y si esta situación no es verdadera podemos poner ":".
let autorizado = usuario === 'Emilia' ? 'Autorizado' : 'No Autorizado';
console.log(autorizado);

// En una sola linea estamos definiendo esta condicional.